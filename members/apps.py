from django.apps import AppConfig

# Have no clue what this is for tbh, delete later probs?
class MembersConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'members'
